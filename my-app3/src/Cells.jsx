
const Cells = ({ cellData }) => {
  return (
    <td>
        {/* cellData is the value gotten from the key,value pairs after changing the object item into an array */}
      {cellData}
    </td>
  )
}

export default Cells
